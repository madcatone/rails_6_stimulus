<p id="notice"><%= notice %></p>

<h1>Events</h1>
<!-- <table data-controller="events" data-info="<%= @events_json %>" data-target="events.model">
--><table>

  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Desc</th>
      <th>No</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr data-controller="events">
        <td>
          <button data-action="click->events#say_hello">Speak!</button>
          <span data-target="events.id"><%= event.id %></span>
          <span data-target="events.output" data-action="events#say_hello"></span>
          <input type="text" name="events[name_with_desc]" id="" data-target="events.nameWithDesc">
        </td>
        <td data-target="events.name"><%= event.name %></td>
        <td data-target="events.desc"><%= event.desc %></td>
        <td data-target="events.no"><%= event.no %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Event', new_event_path %>

<h2>Events with Stimulus</h1>

<div data-controller="events" data-employees-url="/events.json"></div>
<br>